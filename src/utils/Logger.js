// src/utils/Logger.js

export class Logger {
    static info(message) {
        console.info(message);
    }

    static warn(message) {
        console.warn(message);
    }

    static error(message) {
        console.error(message);
    }
}
