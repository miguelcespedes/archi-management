// src/utils/Logger.js

export class Logger {
    static info(message) {
        console.info(message);
    }

    static error(message) {
        console.error(message);
    }

    static warn(message) {
        console.warn(message);
    }
}